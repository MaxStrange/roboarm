language: rust

rust:
  - stable
  - beta
  - nightly

cache: cargo

install:
  - rustup default nightly  # At some point, this will not be necessary
  - sudo apt-get install gcc-arm-none-eabi
  - rustup target add thumbv7em-none-eabihf

script:
  - make ci